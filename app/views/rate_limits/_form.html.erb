<%= form_for(@rate_limit) do |f| %>
  <% if @rate_limit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rate_limit.errors.count, "error") %> prohibited this rate_limit from being saved:</h2>

      <ul>
      <% @rate_limit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
